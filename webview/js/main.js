'use strict';

/* eslint-env browser */


window.addEventListener('load', (e) => {
    document.getElementById('fetch_wpm').addEventListener('click', (c) => {
        // eslint-disable-next-line no-alert
        alert('Fetch the WPM!');
    });
    document.getElementById('canonicalize').addEventListener('click', (c) => {
        // eslint-disable-next-line no-alert
        alert('Convert the WPM using');
    });
});
